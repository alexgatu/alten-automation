# default connection
URL_PROTOCOL=http
URL_HOSTNAME=86.121.249.150
URL_PORT=4999

# DB connection
DB_HOST=localhost
DB_PORT=3306
DB_SCHEMA=alten_auto
DB_USER=root
DB_PASS=alex

# environment settings
CURRENT_ENV=local

# browser running
RUN_BROWSER=chrome

# browser run type
WEB_DRIVER_MANAGER=true

# Headless mode
START_HEADLESS=false